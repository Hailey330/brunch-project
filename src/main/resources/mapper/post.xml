<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.project.brunch.domain.post.PostMapper">
  <update id="UpdateByPost">
    SELECT coverImg FROM post WEHRE id=#{id}
  </update>
  
  <select id="PostList" resultType="com.project.brunch.web.dto.PostDto">
  	SELECT p.id, p.content, p.coverImg, p.createDate, p.postType, p.readCount, p.subTitle, p.title, p.userId , u.nickname
	FROM post p INNER JOIN user u
	ON p.userid = u.id
  </select>
  
  <select id ="findByTodayPost" resultType="com.project.brunch.domain.post.Post">
  	  SELECT * 
  	  FROM post 
  	  WHERE createDate > curdate( )
  </select>
  
  <select id="findByreadCountRank" resultType="com.project.brunch.web.dto.AdminDto">
  	SELECT p.createDate, p.readCount, p.title, u.nickname
	FROM post p INNER JOIN user u
	ON p.userid = u.id
	order by readCount desc
	limit 5
  </select>
  
  <select id="findBy작가의서랍" resultType="com.project.brunch.domain.post.Post">
  	SELECT title, content, coverImg, createDate
  	FROM post 
  	WHERE userId=#{id}
  </select>
  
  <select id="findBy태그" resultType="com.project.brunch.domain.post.Post">
  	SELECT p.title, p.coverImg, p.subTitle, p.content, p.createDate, u.nickname, t.tag
	FROM post p INNER JOIN tag t INNER JOIN user u
	ON t.postId = p.id AND u.Id = p.userId AND t.tag=#{tag}
  </select>
  
</mapper>
